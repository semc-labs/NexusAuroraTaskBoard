/*const { MessageEmbed, MessageActionRow, MessageButton } = require('discord.js');
const { SlashCommandBuilder } = require('@discordjs/builders');

module.exports = {
	data: new SlashCommandBuilder().setName('createTask').setDescription('Fetches helpful documeyyyynt')
	.addStringOption(option =>   
                option.setName("doc")
                        .setDescription("Doc to fetch. If blank return list")
                        .setRequired(false)), 
	async execute(interaction) {
	
	},
};

*/